/*
 * STACK.c
 *
 *  Created on: May 18, 2017
 *      Author: Jocelyn
 */
#include "global.h"
/*
int isEmpty(STACK * s) {

	if(s->top == -1)
		return 1;
	else
		return 0;
}

int isFull(STACK * s) {

	if(s->top == STACKSIZE)
		return 1;
	else
		return 0;
}

int peek(STACK * s) {
	return s->stack[s->top];
}

int pop(STACK * s) {

	int data;

	if(!isempty()) {
		data = (s->stack)[s->top];
		s->top = (s->top) - 1;
		return data;
	}
}

int push(STACK * s, int data) {

	if(!isfull()) {
		(s->top)++;
		(s->stack)[s->top] = data;
	}
}
*/
